# nim.cfg
# this file allows for better imports
# so we can say imoport CONFIG instead of import ../../CONFIG
# if we are a module that is not on the root level                
switch("path", "src")       # Current directory


